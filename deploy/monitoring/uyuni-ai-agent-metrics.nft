table inet uyuni_ai_agent_metrics {
    chain input {
        type filter hook input priority -10; policy accept;

        ip saddr MONITORING_SERVER_IP tcp dport 9898 accept
        tcp dport 9898 counter drop
    }
}

#!/bin/sh
set -eu

# Quadlet publishes the container endpoint only on host loopback. Keeping this
# proxy destination stable prevents container recreation from leaving a
# long-running socket-proxyd process pinned to an obsolete bridge address.
exec /usr/lib/systemd/systemd-socket-proxyd 127.0.0.1:19898
